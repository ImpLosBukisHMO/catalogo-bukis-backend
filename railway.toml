[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd catalogo_backend && gunicorn catalogo_backend.wsgi --bind 0.0.0.0:$PORT --workers 2"
releaseCommand = "cd catalogo_backend && python manage.py migrate --noinput && python manage.py collectstatic --noinput"
